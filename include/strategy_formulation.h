#ifndef STRATEGY_FORMULATION_H
#define STRATEGY_FORMULATION_H

#include <vector>
#include <string>

class StrategyFormulation {
public:
    void formulateStrategy(const std::vector<std::string>& processedData);
};

#endif // STRATEGY_FORMULATION_H
