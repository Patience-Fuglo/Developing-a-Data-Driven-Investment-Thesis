#include "evaluation.h"
#include <iostream>

void Evaluation::evaluatePerformance(const std::vector<std::string>& backtestResults) {
    // Placeholder for performance evaluation logic
    std::cout << "Evaluating performance..." << std::endl;
    for (const auto& data : backtestResults) {
        std::cout << data << std::endl;
    }
}
