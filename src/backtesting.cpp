#include "backtesting.h"
#include <iostream>

void Backtesting::backtestStrategy(const std::vector<std::string>& strategyData) {
    // Placeholder for backtesting logic
    std::cout << "Backtesting strategy..." << std::endl;
    for (const auto& data : strategyData) {
        std::cout << data << std::endl;
    }
}
